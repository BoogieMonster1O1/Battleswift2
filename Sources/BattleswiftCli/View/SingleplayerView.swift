import SwiftTUI

struct SingleplayerView: View {
    var body: some View {
        VStack(alignment: .center) {
            Text("singleplayer yes lol")
            Spacer()
        }
          .frame(maxWidth: 2147483647)
    }
}
